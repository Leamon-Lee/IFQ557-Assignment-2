class ParticipantController:
    def browseEvents(self) -> list:
        return

    def registerEvent(self, participant_id: int, event_id: int) -> int:
        return

    def cancelRegistration(self, participant_id: int, registration_id: int) -> bool:
        return

    def viewTicket(self, participant_id: int, ticket_id: int) -> object:
        return

    def checkIn(self, participant_id: int, ticket_id: int) -> bool:
        return
