class OrganizerController:
    def sendAnnouncement(self, organizer_id: int, event_id: int, message: str) -> bool:
        return

    def createEvent(self, organizer_id: int) -> int:
        return

    def updateEvent(self, organizer_id: int, event_id: int) -> bool:
        return

    def cancelEvent(self, organizer_id: int, event_id: int) -> bool:
        return

    def viewParticipants(self, organizer_id: int, event_id: int) -> list:
        return
